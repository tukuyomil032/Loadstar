# Open Decisions and Implementation Gates

The high-level product and platform direction is closed in ADR-0001 through ADR-0017. Remaining work is execution and evidence, not another broad questioning phase.

1. Inventory every reference file, command, provider, guard, failure, cancel, retry, partial-success, recovery, and persisted-result branch.
2. Produce schema migration mapping and read-only fallback behavior for every persisted field.
3. Register every exact proxy/artifact row with official source, version, loader, config path, managed key, artifact identity, and SHA-256.
4. Finalize per-screen VoiceOver wording, focus order, keyboard matrix, and acceptance examples.
5. Select Visualize/image-generation screens after screen contracts and acceptance criteria are stable.
6. Execute Phases 00 through 10 and attach the required evidence layer to each parity row.

Do not invent behavior for an unresolved gate. A genuinely independent decision may create a new ADR only after checking the active map.
